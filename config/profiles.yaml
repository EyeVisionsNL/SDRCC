profiles:

  weather:
    name: Weather / METEOR
    managed_by: sdrcc
    device: sdr2
    description: "METEOR-M2 3/4 ontvangst via SatDump"

  adsb:
    name: ADS-B
    managed_by: external
    device: sdr2
    description: "ADS-B draait al buiten SDRCC; SDRCC mag dit later netjes detecteren"

  lora:
    name: LoRa / MeshCore
    managed_by: external
    device: external
    description: "MeshCore draait via bestaande integratie; SDRCC toont later alleen status"

  aprs:
    name: APRS
    managed_by: sdrcc
    device: sdr2
    description: "Toekomstige APRS-module"

  manual:
    name: Manual / Lab
    managed_by: user
    device: sdr2
    description: "Handmatig gebruik van SDR2"
